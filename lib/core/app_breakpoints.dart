class AppBreakpoints {
  AppBreakpoints._();

  // layouts compactos de cards
  static const double small = 600;

  // navegação e rodapé
  static const double medium = 768;

  // conteúdo extenso
  static const double large = 900;
}
